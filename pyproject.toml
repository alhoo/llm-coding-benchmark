[project]
name = "llm-coding-benchmark"
version = "0.1.0"
description = ""
authors = [
    {name = "Lasse Hyyrynen",email = "lasse.hyyrynen@anomalia.io"}
]
readme = "README.md"
requires-python = ">=3.12,<4.0"
dependencies = [
    "langchain-openai (>=1.1.11,<2.0.0)",
    "langchain-anthropic (>=1.3.4,<2.0.0)",
    "langchain-google-genai (>=2.0.0)",
    "langchain (<1.2.0)",
    "pytest (>=8.0)",
    "pytest-json-report (>=1.5.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
